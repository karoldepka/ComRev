# Credentials for each database id defined in databases.toml.
# Copy to databases.secrets.toml and fill in real values — DO NOT commit that file.

[postgres-1]
username = "postgres"
password = "your_password_here"

# [postgres-2]
# username = "postgres"
# password = "your_password_here"

# [mongo-1]
# username = "mongouser"
# password = "mongopass"

# [surreal-local]
# username = "admin"
# password = "your_surreal_password"

# [surreal-cloud]
# username = "root"
# password = "surreal_secret"

# [couch-1]
# username = "admin"
# password = "couch_secret"

# Extra environment variables set at startup (only if not already in the environment).
# Use this for API keys, service tokens, and other non-connection secrets.
[extra_env]
# SUPABASE_URL              = "https://your-project.supabase.co"
# SUPABASE_SERVICE_ROLE_KEY = "eyJ..."
# GH_PAT                    = "ghp_..."
