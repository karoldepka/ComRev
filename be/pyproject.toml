[project]
name = "be"
version = "0.1.0"
description = "FastAPI backend for Vercel"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[project.scripts]
app = "app.main:app"

[tool.uv]
dev-dependencies = []