[tool.poetry]
package-mode = false
name = "comrev"
version = "0.1.0"
description = ""
authors = ["you"]

[tool.poetry.dependencies]
python = "^3.13"
httpx = "^0.27"
pyyaml = "^6.0"
tenacity = "^8.3"
python-dotenv = "^1.2.2"
gitpython = "^3.1.49"

[tool.poetry.group.dev.dependencies]

# Optional local CLI entrypoint (NOT used by Vercel runtime)
[tool.poetry.scripts]
app = "comrev.main:app"
