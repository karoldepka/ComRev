[tool.poetry]
name = "package-tracker"
version = "0.1.0"
description = ""
authors = ["you"]

[tool.poetry.dependencies]
python = "^3.13"
httpx = "^0.27"
pyyaml = "^6.0"
tenacity = "^8.3"
# supabase = "^2.6"

[tool.poetry.group.dev.dependencies]
# ruff = "^0.6"

